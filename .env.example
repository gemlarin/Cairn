# Get a free key: https://www.nps.gov/subjects/developer/get-started.htm
# Do not prefix with VITE_. The Vite proxy reads this. It must not ship to the browser.
NPS_API_KEY=

# Supabase → Project Settings → API (publishable / anon key only — never service_role)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
